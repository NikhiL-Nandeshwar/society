/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Optional: enable any new Next.js features you want to try
  },
  turbopack: {}, // Enable Turbopack for faster builds
};

module.exports = nextConfig;
